function esPositivo(num) {
    // Verificar si el número es positivo, negativo o cero
    if (num > 0); {
        return "Es positivo";
      } else if (num < 0) {
        return "Es negativo";
      }  else {
        return false;}
  }
console.log(esPositivo(10))  